#include<stdio.h>
#include<string.h>

int media(char *comando){
	if(comando == 'A' || strcmp(comando, "ARITMETICA") == 0 || strcmp(comando, "aritmetica") == 0){
		
	}
	if(comando == 'P' || strcmp(comando, "PONDERADA") == 0 || strcmp(comando, "ponderada") == 0){
	
	}
}

int main(){
	
	int mediaf;
	char comando[];	
	scanf("%s", &);

	mediaf = media(&comando);
	
	printf("%d\n", mediaf);

	return 0;
}
